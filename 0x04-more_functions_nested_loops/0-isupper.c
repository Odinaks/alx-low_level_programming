#include "main.h"
#include <stdio.h>

/**
 * _isupper - function that checks for uppercase charaters
 * @c: character to check
 * Return: Always 0 or 1
 */

int _isupper(int c);
{
	return(c >= 65 && c <= 90)
}

#include "main.h"
#include <stdio.h>

/**
 * mul - a function that Multiplies two integers.
 * @a: first number
 * @b: second number
 * Return: the product of a and b
 */ 

int mul(int a, int b);
{
	return (a * b);
}
